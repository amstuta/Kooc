#include <stdlib.h>
typedef struct _kc_Object Object;
typedef struct _kc_vt_Object vt_Object;

struct _kc_Object
{
	void *vt;
	char *name;
};


@module Object
{
	const char *name_of_interface();
	Object *alloc();
	Object *new();
	void init();
	void clean();
	void delete();
	int isKindOf(const char*);
	int isKindOf(Object *);
	int isInstanceOf(const char *);
	int isInstanceOf(Object *);
}


@implementation Object
{
	void clean()
	{	
	}

	int isKindOf(const char *name)
	{
	}

	int isKindOf(Object *a)
	{
	}

	int isInstanceOf(const char *name)
	{
	}

	int isInstanceOf(Object *a)
	{
	}
}